

pgrep bash

cat /proc/$PID/environ | tr '\0' '\n'

var=value
echo $var
echo ${var}

echo "We have ${var} defined" # to use in ""
echo '$var' # will print $var

# add to path
export PATH="$PATH:/home/user/bin"

# length of var
lenght = ${#var}

# add to path 
# using ${parameter:+expr}
prepend() { [-d "$2" ] && eval $1=\"$2\$\{$1:+'"'\$$1\}\" && export $1 ; }



